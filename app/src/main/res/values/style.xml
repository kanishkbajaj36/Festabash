<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="CustomDialogTheme">
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="text_dark_medium_14">
        <item name="android:textSize">14sp</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">normal</item>

    </style>

   <!-- <style name="primary_button">
        <item name="android:textSize">16dp</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:stateListAnimator">@null</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:fontFamily">@font/heebo</item>
        <item name="android:background">@drawable/custom_primary_button</item>
        <item name="android:translationZ">2dp</item>
    </style>-->

    <style name="YourMonthTextAppearance" parent="TextAppearance.AppCompat.Title">
        <item name="android:textColor">@color/black</item>
    </style>


    <style name="AppTheme.PopupMenu" parent="@style/Widget.AppCompat.PopupMenu">
        <!-- Add any customizations here -->
        <item name="android:popupBackground">@drawable/popup_menu_background</item>
        <!-- Add other attributes as needed -->
    </style>


    <style name="PopupMenuStyle" parent="@style/Widget.AppCompat.PopupMenu">
        <item name="android:popupBackground">@drawable/custom_popup_background</item>
        <item name="android:dropDownVerticalOffset">16dp</item> <!-- Adjust top margin -->
        <item name="android:dropDownWidth">200dp</item> <!-- Adjust width -->
        <item name="android:popupElevation">8dp</item> <!-- Add elevation for shadow effect --><!-- Add padding -->
    </style>

</resources>